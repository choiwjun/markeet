/**
 * Supabase 자동 생성 타입 파일
 *
 * 이 파일은 Supabase CLI를 통해 자동 생성됩니다.
 * 수동으로 수정하지 마세요.
 *
 * 타입 생성 방법:
 * 1. Supabase CLI 설치: npm install -g supabase
 * 2. Supabase 로그인: supabase login
 * 3. 타입 생성:
 *    - 원격 DB: npm run supabase:types (SUPABASE_PROJECT_ID 환경변수 필요)
 *    - 로컬 DB: npm run supabase:types:local
 *
 * 현재는 수동으로 정의된 types/database.ts를 사용합니다.
 * Supabase 프로젝트 설정 후 이 파일을 자동 생성하세요.
 */

// Re-export from database.ts for compatibility
export * from './database';
